
// Root component, conecta todos los componentes y rutas de la aplicación
import { AppRouter } from '@app/router'

// Componente raíz — renderiza el sistema de rutas de la aplicación
export function App() {
  return <AppRouter />
}